{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Servidor mock com JSON Server",
  "scripts": {
    "start": "json-server --watch ../mock-data/bd.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
